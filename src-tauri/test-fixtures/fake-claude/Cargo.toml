[package]
name = "fake-claude"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "fake-claude"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2", default-features = false, features = ["tls"] }
