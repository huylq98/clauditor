# fake-claude

Test-only impersonator for the real `claude` CLI. Replays a JSON scenario.
Built and used by the e2e suite — never shipped in releases.
See `tests/e2e/fixtures/scenarios/` for example scenarios.
